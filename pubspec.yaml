name: sartex
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+16

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^1.1.0
  bloc: ^9.0.0
  flutter_bloc: ^9.1.0
  uuid: ^4.4.2
  flutter_svg: ^2.0.2
  shared_preferences: ^2.0.18
  freezed: ^3.0.6
  json_annotation: ^4.8.0
  syncfusion_flutter_datagrid: ^29.1.37
  table_calendar: ^3.0.9
  syncfusion_flutter_datagrid_export: ^29.1.37
  excel: ^4.0.3
  text_scroll: ^0.2.0
  flutter_html: ^3.0.0-beta.2
  intl: ^0.20.2
  webview_flutter: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.1

flutter:
  fonts:
    - family: Agency FB
      fonts:
        - asset: assets/fonts/agencyfb_reg.ttf
    - family: NotoSansArmenian
      fonts:
        - asset: assets/fonts/NotoSansArmenianRegular.ttf
    - family: Sylfaen
      fonts:
        - asset: assets/fonts/Sylfaen.ttf

  uses-material-design: true
  assets:
    - assets/

